@page "/admin/UrlNamePlaceholder-list"
@rendermode InteractiveServer
@inject AuthenticationStateProvider GetAuthenticationStateAsync
@inject ICastMemberMovieAdminService CastMemberMovieAdminService
@attribute [StreamRendering]

<PageTitle>Cast Member Movie List</PageTitle>

<section>
  <h1>Cast Member Movie List</h1>
  <article>
    <table class="table table-striped table-sm">
      <thead>
        <tr>
          <th scope="col">Id</th>

          @* ListHeaderCodePlaceholder *@

          <th scope="col">Actions</th>
        </tr>
      </thead>
      <tbody>
        @if (TableLowercaseNamePlaceholder == null)
        {
          <tr>
            @* LoadingCountCodePlaceholder *@
          </tr>
        }
        else if (TableLowercaseNamePlaceholder.Count == 0)
        {
          <tr>
            @* ZeroCountCodePlaceholder *@
          </tr>
        }
        else
        {
          @foreach (var castMemberMovie in TableLowercaseNamePlaceholder)
          {
            <tr>
              <td>@castMemberMovie.Id</td>

              @* ListItemCodePlaceholder *@

              <td>
                <a href="@("/admin/UrlNamePlaceholder-edit/" + castMemberMovie.Id)">Edit</a>
              </td>
            </tr>
          }
        }
      </tbody>
    </table>
    <a href="/admin/UrlNamePlaceholder-edit">Create</a>
  </article>
</section>

@code {
  List<CastMemberMovie>? TableLowercaseNamePlaceholder = null;

  protected override async Task OnInitializedAsync()
  {
    var authState = await GetAuthenticationStateAsync.GetAuthenticationStateAsync();
    var userName = authState.User.Identity?.Name;

    if (userName == null)
    {
      return;
    }

    TableLowercaseNamePlaceholder = await CastMemberMovieAdminService.GetAllAsync(userName);
  }
}
